// request is mod that will have all the wrapper for besozzi
// use hyper::Client;

// fn get() {
//     let client = Client::new();
//     let uri = "http://httpbin.org/ip".parse()?;
//     let mut resp = client.get(uri).await?;
//     println!("Response: {}", resp.status());
// }
